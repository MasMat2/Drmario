
create pygame interface
create player object
create block objects





check if valid move
    if tail change valid
        add change tail to queue
    if move valid
        add move to queue

for events in queue
    execute events

render blocks
render player
